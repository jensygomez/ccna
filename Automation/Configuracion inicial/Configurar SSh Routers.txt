enable
configure terminal

hostname Router-01
ip domain-name gns3.lab
enable secret cisco

line console 0
 password cisco
 login
 logging synchronous
 exec-timeout 5 0
 exit

username cisco privilege 15 secret cisco
username user privilege 1 secret user123

crypto key generate rsa modulus 2048
ip ssh version 2
ip ssh time-out 60
ip ssh authentication-retries 2


! =======================================
! =    SISTEMA DE LABORATORIO GNS3      =
! =    ACCESO NO AUTORIZADO PROHIBIDO   =
! =======================================
!
! Este sistema es solo para uso autorizado.
! Todo acceso está siendo monitoreado y registrado.
! ----------------------------------------

line vty 0 15
 login local
 transport input ssh telnet
 exec-timeout 10 0
 session-timeout 10
 absolute-timeout 30
 exit

logging buffered 8192 debugging
logging console debugging
logging monitor debugging
service tcp-keepalives-in
service tcp-keepalives-out

archive
 log config
  logging enable
  notify syslog
  hidekeys
 exit

ip access-list standard PROTECT-VTY
 permit 192.168.0.0 0.0.0.255
 permit 192.168.1.0 0.0.0.255
 deny any log

line vty 0 15
 access-class PROTECT-VTY in
 exit

aaa new-model
aaa authentication login SSH-AUTH local
aaa authorization exec SSH-AUTH local

service timestamps debug datetime msec localtime show-timezone
service timestamps log datetime msec localtime show-timezone

end
write memory